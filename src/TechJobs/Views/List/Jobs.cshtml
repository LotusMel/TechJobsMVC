<h1>@ViewBag.title</h1>
<h3>@ViewBag.jobs.Count Result(s)</h3>

<form action="/Search/Results" > 
    <p>

        @foreach (Dictionary<string, string> job in ViewBag.jobs)
        // TODO #3 - Use the same list display from Views/Search/Index.cshtml foreach (KeyValuePair<string, string> column in ViewBag.columns)
        {
            {
                foreach (KeyValuePair<string, string> item in job)
                {
                    <table link href="~/css/site.css" rel="stylesheet">
                        <tr>
                            <td id="@item.Key" value="@item.Key">@item.Key</td>
                            <td id="@item.Key" value="@item.Value">@item.Value</td>
                         </tr>
                    </table> 
                }
                    <br />
            }
        }



    </p>

    <p>
        <label for="searchTerm">Keyword:</label>
        <input type="text" name="searchTerm" id="searchTerm" />
    </p>

    <input type="submit" value="Search" />
</form>

<hr />

@if (ViewBag.jobs != null)
{

    <h3>@ViewBag.jobs.Count Result(s)</h3>

    // TODO #2 - Loop over jobs dictionary and display all job fields
}